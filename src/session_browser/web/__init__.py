"""Web presentation package for routes, presenters, renderers, and templates.

The HTTP serving layer imports this package when building page routes and view
models. It keeps browser-facing helpers separate from indexing and source
normalization code.
"""
