# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "feipi-session-browser-java"

[setup]
script = '''
set -eu

echo "LOCAL_ENV_INITIAL_PWD=$PWD"

repo_root="$(git rev-parse --show-toplevel 2>/dev/null)" || {
  echo "BLOCKED_LOCAL_ENV_NOT_IN_GIT_CHECKOUT: pwd=$PWD" >&2
  exit 2
}

cd "$repo_root"
echo "LOCAL_ENV_REPO_ROOT=$repo_root"

if [ ! -f pyproject.toml ]; then
  echo "BLOCKED_LOCAL_ENV_MISSING_PYPROJECT: repo_root=$repo_root" >&2
  exit 2
fi

if [ ! -f uv.lock ]; then
  echo "BLOCKED_LOCAL_ENV_MISSING_UV_LOCK: repo_root=$repo_root" >&2
  exit 2
fi

uv sync --frozen'''
