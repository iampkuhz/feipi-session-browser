@startuml
title Gate 计划、执行、证据与重跑流程
/'
top to bottom direction
activity syntax defaults to this direction.
'/
skinparam shadowing false
skinparam nodesep 35
skinparam ranksep 55
skinparam activity {
  BackgroundColor #F8FAFC
  BorderColor #475569
  FontColor #0F172A
}

start
:S1 捕获输入快照;
note right
  输入只来自 Git、--base 或 --changed-files。
  plan 空输入返回空计划；run 空输入写入 input-empty receipt。
end note
:S2 匹配 Trigger;
note right
  file → pattern → Gate
  未匹配只表示 NOT_TRIGGERED
end note
:S3 冻结 GatePlan;
note right
  固定输入、匹配、Gate、RecipeStep、CommandInvocation、
  进程数和时间目标。
end note
:S4 执行 GatePlan;
note right
  PLAN / START / HEARTBEAT / STALL / RESULT / DONE
end note
:S5 分类执行状态;
note right
  PASS / BLOCKED verification-failed / FAIL
end note
:S6 写入不可变收据;
note right
  schema v5，run-id 目录不可覆盖。
end note
:S7 生成规范重跑;
note right
  固定 HEAD、base 与 content fingerprint。
end note
stop
@enduml
