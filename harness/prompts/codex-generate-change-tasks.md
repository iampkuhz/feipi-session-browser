# Codex Prompt: Generate OpenSpec Task Files

Read the active OpenSpec change and split it into serial task files under `tasks/changes/<change-id>/`.

Each task must include validation commands and acceptance criteria. Do not implement code.
