# harness/context/routes.yaml
# Context routes: 按路径和语义特征加载最小上下文。
# 禁止全量加载 docs/skills 作为 fallback。
version: 1
routing_strategy: staged_minimal_context

routes:
  - id: task-intake
    match:
      intent_any:
        - planning
        - unknown
        - cross_module
    load:
      - AGENTS.md
      - harness/README.md
      - settings.gradle.kts
      - README.md
    do_not_load:
      - docs/**
      - historical_reports/**
      - real_session_data/**
      - tmp/**

  - id: harness-agent-rules
    match:
      path_any:
        - harness/**
        - skills/**
        - scripts/harness/**
        - scripts/checks/**
        - AGENTS.md
        - CLAUDE.md
    load:
      - AGENTS.md
      - harness/README.md
      - harness/manifest.yaml
      - harness/agent-runtime.manifest.yaml
      - docs/agent-runtime.md
    load_after:
      - relevant validator scripts only
      - relevant harness workflow docs only
    do_not_load:
      - docs/** except explicitly listed docs/agent-runtime.md
      - historical_reports/**
      - real_session_data/**

  - id: java-source-ingest
    match:
      path_any:
        - java/source-spi/**
        - java/sources/**
        - java/scan-engine/**
    load:
      - java/source-spi targeted interfaces only
      - related provider adapter files only
      - related contract tests only
      - relevant openspec spec only
    do_not_load:
      - java/core-domain/**
      - java/normalization-engine/**
      - docs/**
      - historical_reports/**

  - id: java-normalization-token
    match:
      path_any:
        - java/normalization-engine/**
    load:
      - java/normalization-engine targeted files only
      - related golden fixtures
      - related contract tests
    do_not_load:
      - java/sources/**
      - docs/**

  - id: java-domain-core
    match:
      path_any:
        - java/core-domain/**
        - java/application/**
        - java/index-api/**
        - java/index-store-sqlite/**
    load:
      - java/core-domain targeted files only
      - java/application targeted files only
      - java/index-api targeted files only
      - openspec specs only if Java source change
    do_not_load:
      - java/sources/**
      - docs/**

  - id: java-cli-web
    match:
      path_any:
        - java/app-cli/**
        - java/web/**
        - java/tests/architecture/**
    load:
      - README.md
      - related Java CLI/web files only
      - related tests only
    do_not_load:
      - java/core-domain/**
      - java/sources/**
      - docs/**
      - historical_reports/**

  - id: java-contract-tests
    match:
      path_any:
        - java/tests/contracts/**
        - java/tests/support/**
    load:
      - related contract test files only
      - related SPI interfaces only
      - relevant golden fixtures only
    do_not_load:
      - java/sources/**
      - docs/**
      - historical_reports/**
