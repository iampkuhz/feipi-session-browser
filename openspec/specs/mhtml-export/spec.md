# MHTML Export Spec

## Requirements

### Requirement: Self-contained session export

The system SHALL export a session page as a self-contained document that preserves current-session view switching.
