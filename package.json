{
  "name": "feipi-session-browser-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "Playwright E2E tests for feipi session browser",
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  },
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  }
}
