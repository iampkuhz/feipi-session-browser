default_stages: [pre-commit]

repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.18
    hooks:
      - id: ruff-format
        types_or: [python, pyi]
      - id: ruff
        args: ["--fix"]
        types_or: [python, pyi]

  - repo: local
    hooks:
      - id: pyright
        name: pyright
        entry: bash -c './scripts/session-browser.sh type'
        language: system
        types_or: [python, pyi]
        pass_filenames: false

      - id: python-docstrings
        name: python docstrings
        entry: bash -c './scripts/session-browser.sh doc'
        language: system
        types_or: [python, pyi]
        pass_filenames: false

      - id: python-security
        name: python security
        entry: bash -c './scripts/session-browser.sh audit'
        language: system
        files: ^(pyproject\.toml|requirements(-dev)?(\.lock|\.txt)?)$
        pass_filenames: false

      - id: python-complexity
        name: python complexity
        entry: bash -c './scripts/session-browser.sh complexity'
        language: system
        types_or: [python, pyi]
        pass_filenames: false

      - id: python-dead-code
        name: python dead code
        entry: bash -c './scripts/session-browser.sh dead-code'
        language: system
        types_or: [python, pyi]
        pass_filenames: false

      - id: python-deps
        name: python dependencies
        entry: bash -c './scripts/session-browser.sh deps-check'
        language: system
        files: ^(pyproject\.toml|requirements(-dev)?(\.lock|\.txt)?)$
        pass_filenames: false
