/**
 * Jakarta Validation 约束注解和统一校验入口。
 *
 * <p>本包提供：
 *
 * <ul>
 *   <li>{@link com.feipi.session.browser.validation.ValidationSupport} — 统一校验入口，支持 bean 校验和 record
 *       canonical constructor 参数校验。
 *   <li>{@link com.feipi.session.browser.validation.Finite} — 浮点有限值约束注解。
 *   <li>{@link com.feipi.session.browser.validation.Ratio} — 比率约束注解，范围 [0.0, 1.0]。
 * </ul>
 */
package com.feipi.session.browser.validation;
