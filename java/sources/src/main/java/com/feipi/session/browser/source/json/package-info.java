/**
 * JSON/JSONL source 候选项解析与映射辅助组件。
 *
 * <p>共享的底层 JSONL reader 工具位于 {@code com.feipi.session.browser.source.common}。本包保留内置 JSON 形态
 * provider 使用的 source 候选解析模板与 JSON event 到 record 的映射器。
 */
package com.feipi.session.browser.source.json;
